{{ 'section-lp-hawaiian.css' | asset_url | stylesheet_tag }}

<section class="lp-hawaiian lp-hawaiian-hero section-{{ section.id }}-padding">
  <div class="lp-hawaiian__wrap">
    <div class="lp-hawaiian-hero__grid">
      <div class="lp-hawaiian-hero__content">
        {%- if section.settings.eyebrow != blank -%}
          <span class="lp-hawaiian__eyebrow">{{ section.settings.eyebrow | escape }}</span>
        {%- endif -%}
        {%- if section.settings.heading != blank -%}
          <h1 class="lp-hawaiian__title">{{ section.settings.heading | escape }}</h1>
        {%- endif -%}
        {%- if section.settings.subheading != blank -%}
          <p class="lp-hawaiian__subtitle">{{ section.settings.subheading | escape }}</p>
        {%- endif -%}
        {%- if section.settings.button_label != blank -%}
          <a href="{{ section.settings.button_link }}" class="lp-hawaiian__btn">
            {{ section.settings.button_label | escape }}
          </a>
        {%- endif -%}
        {%- if section.blocks.size > 0 -%}
          <div class="lp-hawaiian-hero__badges">
            {%- for block in section.blocks -%}
              <div class="lp-hawaiian-hero__badge" {{ block.shopify_attributes }}>
                <span>✓</span> {{ block.settings.text | escape }}
              </div>
            {%- endfor -%}
          </div>
        {%- endif -%}
      </div>
      <div class="lp-hawaiian-hero__media">
        {%- if section.settings.image != blank -%}
          {{
            section.settings.image
            | image_url: width: 1200
            | image_tag: loading: 'eager', fetchpriority: 'high', alt: section.settings.heading
          }}
        {%- else -%}
          {{ 'product-apparel-2' | placeholder_svg_tag: 'placeholder-svg' }}
        {%- endif -%}
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "LP Hawaiian — Hero",
  "tag": "section",
  "class": "section",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Surtitre", "default": "Personnalisation photo" },
    { "type": "text", "id": "heading", "label": "Titre", "default": "Votre chemise hawaïenne unique, avec votre chat" },
    { "type": "textarea", "id": "subheading", "label": "Sous-titre", "default": "Téléversez votre photo et créez une chemise tropicale personnalisée — le cadeau parfait qui surprendra à coup sûr." },
    { "type": "text", "id": "button_label", "label": "Bouton", "default": "Personnaliser ma chemise" },
    { "type": "url", "id": "button_link", "label": "Lien du bouton" },
    { "type": "image_picker", "id": "image", "label": "Image hero" }
  ],
  "blocks": [
    {
      "type": "badge",
      "name": "Badge confiance",
      "settings": [
        { "type": "text", "id": "text", "label": "Texte", "default": "Livraison suivie" }
      ]
    }
  ],
  "presets": [
    {
      "name": "LP Hawaiian — Hero",
      "blocks": [
        { "type": "badge", "settings": { "text": "Impression HD premium" } },
        { "type": "badge", "settings": { "text": "Livraison internationale" } },
        { "type": "badge", "settings": { "text": "Satisfait ou remboursé 30 jours" } }
      ]
    }
  ]
}
{% endschema %}
